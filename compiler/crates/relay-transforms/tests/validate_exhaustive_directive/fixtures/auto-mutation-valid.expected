==================================== INPUT ====================================
mutation AutoValid {
  unionField {
    ... on A { __typename }
    ... on B { __typename }
  }
}
# %extensions%
extend type Mutation { unionField: MyUnion }
type A { id: ID }
type B { id: ID }
union MyUnion = A | B
==================================== OUTPUT ===================================
mutation AutoValid {
  unionField {
    ... on A {
      __typename
    }
    ... on B {
      __typename
    }
  }
}
